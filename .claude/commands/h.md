---
description: Run a prompt (Haiku in Claude Code; current model in other harnesses)
argument-hint: <prompt>
model: haiku
---
Run the following prompt. Claude Code's command metadata selects Haiku; a harness
that does not support that metadata should use its current model.

$ARGUMENTS
