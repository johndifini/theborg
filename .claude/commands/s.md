---
description: Run a prompt (Sonnet in Claude Code; current model in other harnesses)
argument-hint: <prompt>
model: sonnet
---
Run the following prompt. Claude Code's command metadata selects Sonnet; a harness
that does not support that metadata should use its current model.

$ARGUMENTS
