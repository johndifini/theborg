---
description: Run a prompt (Opus in Claude Code; current model in other harnesses)
argument-hint: <prompt>
model: opus
---
Run the following prompt. Claude Code's command metadata selects Opus; a harness
that does not support that metadata should use its current model.

$ARGUMENTS
