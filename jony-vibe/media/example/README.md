# Example Media Project

This is a synthetic template. Copy its structure into a private project directory and replace every placeholder.

## Approved Final

Asset: `<approved-asset.ext>`

- Intended use: `<placement or deliverable>`
- Dimensions: `<width × height>`
- Format: `<file format>`
- Checksum: `<algorithm and digest, when useful>`
- Exact text: `<verbatim copy>`
- Palette: `<color values>`
- Composition: `<layout and visual hierarchy>`
- Required elements: `<elements that must remain>`
- Exclusions: `<elements, treatments, or references to avoid>`

## Modification Prompt

Edit the supplied approved asset while preserving `<invariants>`. Change only:
`<requested change>`. Do not introduce `<exclusions>`.

## Recreation Prompt

Create `<asset type and aspect ratio>` for `<generic purpose>`. Use `<palette>`,
`<composition>`, and the exact text `<verbatim copy>`. Include `<required
elements>`. Keep the result `<visual character>`. Do not use `<exclusions>`.

## Provenance

- Creation method: `<tool or workflow>`
- Source files: `<paths within this project>`
- Selected version: `<filename or version identifier>`
- Selection notes: `<why this version was approved>`
