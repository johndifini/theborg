---
name: readonly-scheduled-tasks
description: "When authoring or running a scheduled task whose .conf sets REPORT=1 (the read-only kind, editing .bin/run-scheduled-task.sh or a .claude/scheduled/*.conf): it has no Bash, no state gate and cannot email itself, so the runner's generic preamble does not apply to it."
---

<!-- Generated by The Borg rule bridge. Edit the canonical .claude/rules source, not this file. -->

Read `../../../.claude/rules/readonly-scheduled-tasks.md` — the path is relative to this SKILL.md — and
follow it in full. That file is the canonical rule. Claude Code loads it
automatically from `.claude/rules/`; this skill exists only so Codex reaches the
same text from the same directory scope.
