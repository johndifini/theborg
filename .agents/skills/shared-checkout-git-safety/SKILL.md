---
name: shared-checkout-git-safety
description: "When running git in this workspace: a sibling agent session may be editing the same checkout — never use tree-wide commands (stash, reset --hard, checkout .) to inspect state, and re-derive branch relationships immediately before acting on them."
---

<!-- Generated by The Borg rule bridge. Edit the canonical .claude/rules source, not this file. -->

Read `../../../.claude/rules/shared-checkout-git-safety.md` — the path is relative to this SKILL.md — and
follow it in full. That file is the canonical rule. Claude Code loads it
automatically from `.claude/rules/`; this skill exists only so Codex reaches the
same text from the same directory scope.
