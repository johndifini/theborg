---
name: scheduled-state-writes
description: "When a scheduled task writes its state or plan file under .claude/scheduled/state/: Edit and Write are blocked by the harness's generic .claude/ sensitive-file heuristic, not by any deny rule. Route the write through Bash; do not conclude the write is forbidden."
---

<!-- Generated by The Borg rule bridge. Edit the canonical .claude/rules source, not this file. -->

Read `../../../.claude/rules/scheduled-state-writes.md` — the path is relative to this SKILL.md — and
follow it in full. That file is the canonical rule. Claude Code loads it
automatically from `.claude/rules/`; this skill exists only so Codex reaches the
same text from the same directory scope.
