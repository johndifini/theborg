---
name: tracked-agent-files-are-public
description: "When writing an agent's .claude/scheduled/*.prompt or .claude/commands/*.md: those paths are tracked in a PUBLIC repo, so carry the method and cite the private facts by reference instead of inlining them."
---

<!-- Generated by The Borg rule bridge. Edit the canonical .claude/rules source, not this file. -->

Read `../../../.claude/rules/tracked-agent-files-are-public.md` — the path is relative to this SKILL.md — and
follow it in full. That file is the canonical rule. Claude Code loads it
automatically from `.claude/rules/`; this skill exists only so Codex reaches the
same text from the same directory scope.
