# ---------------------------------------------------------------------------
# .env.example — template for The Borg's single consolidated env file.
# THIS IS A TEMPLATE — values are fake. Copy and fill in:
#
#   cp .env.example .env
#   chmod 600 .env
#   $EDITOR .env
#
# The filled-in .env stays at the workspace root, chmod 600, and is gitignored
# (`.env` rule in .gitignore). NEVER commit it — this .example (keys + docs,
# no real values) is the only version that belongs in version control.
#
# Consumers:
#   .bin/notify-email.sh — EMAIL_* (Gmail SMTP)
#
# NOT here by design: CLAUDE_CODE_OAUTH_TOKEN lives in ~/.zshenv (chmod 600) so
# interactive shells and launchd jobs (which source ~/.zshenv via
# .bin/run-scheduled-task.sh) authenticate the Claude CLI the same way.
#
# notify-email.sh parses KEY=VALUE line-by-line (it does NOT source the file),
# so do not put inline `# comments` after a value — they'd be read as part of it.
# ---------------------------------------------------------------------------

# ── Email notifications (.bin/notify-email.sh) ──────────────────────────────
# EMAIL_SMTP_PASS must be a Gmail APP PASSWORD, not the account password.
# Create one at https://myaccount.google.com/apppasswords (requires 2FA on the
# sending account). The 16-char value's spaces are cosmetic — notify-email.sh
# strips all whitespace, so paste-with-spaces is fine.

# Gmail address used to authenticate.
EMAIL_SMTP_USER=you@gmail.com

# Gmail App Password (16 chars). NOT your account login password.
EMAIL_SMTP_PASS=xxxxxxxxxxxxxxxx

# From address shown on the email. Defaults to EMAIL_SMTP_USER if omitted.
EMAIL_FROM=you@gmail.com

# Where notifications are delivered.
EMAIL_TO=recipient@example.com

# Optional — these are the defaults; uncomment only to override.
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
