# Backlog

- [ ] **Add HTML support to email notifications** (c4po, added 2026-07-27) — `.bin/notify-email.sh` sends every agent notification as `Content-Type: text/plain; charset=UTF-8` with a single `printf`-built message, so the Markdown the scheduled `.prompt` files pipe into it (headings, bullets, links, tables from the security/lint/assumptions audits, dream proposals, market scans, social drafts) arrives as raw text. Add HTML output: emit `multipart/alternative` with the existing plain-text body as one part and a rendered HTML part as the other, so plain-text clients keep working. Decisions to make: whether the script renders Markdown→HTML itself (no Pandoc/Node dependency on the headless Mac Studio — likely a small converter or having callers pass HTML), whether it's opt-in via a flag/env var or the default for all callers, and how the resume footer (`— To continue this session…`) renders in both parts. All current callers pipe bodies on stdin and must keep working unchanged: `.bin/run-scheduled-task.sh`, `c4po/.claude/scheduled/*.prompt` (security-audit, lint-audit-monthly, assumptions-audit-monthly, dream, backlog-burndown), `warren-bot-fett/.claude/scheduled/*.prompt`, `mrs-beast/.claude/scheduled/mrs-beast-social-media-drafts.prompt`. Per the workspace rule, route the visual styling of the HTML template (typography, color, layout) through `jony-vibe/`.

- [ ] **Route repo-scoped work to each repo's own BACKLOG.md** (workspace, added 2026-07-26) — `/backlog` (`.claude/commands/backlog.md`) hardcodes the workspace root `BACKLOG.md` as "the single shared backlog file for the whole workspace" and says "Do not create per-agent backlog files", but each independent repo under `repos/` already keeps its own (e.g. `repos/waiq/BACKLOG.md`, 92 lines with P1–P4 tiers) and `c4po/.claude/scheduled/c4po-backlog-burndown.prompt` already enumerates `${BORG_ROOT}/BACKLOG.md` plus every `${BORG_ROOT}/repos/*/BACKLOG.md`. Update the command to state the rule the workspace actually follows: work scoped to a repo under `repos/` goes in that repo's `BACKLOG.md` (matching that file's existing format/priority tiers), workspace and agent work goes in the root `BACKLOG.md`; per-agent backlog files (e.g. `c4po/BACKLOG.md`) remain forbidden. Command must resolve the target by which repo the work belongs to, not by cwd, and check for duplicates in the target file. Then update `README.md` line 75 (the `/backlog` row still describes it as the shared workspace backlog only) and run `.bin/sync-codex-prompts.sh --check`. Also relocate the existing misfiled entry: the "Let agent sessions run signed iOS device builds without handing the build to John" item at the top of the workspace `BACKLOG.md` is waiq-scoped and belongs in `repos/waiq/BACKLOG.md` — it overlaps the existing waiq P1 "KokoroSwift's resource bundle can't be codesigned" signing item, so merge or cross-reference rather than duplicating.

- [ ] **Let agent sessions run signed iOS device builds without handing the build to John** (c4po, added 2026-07-26) — Today every code change to `repos/waiq`'s iOS app costs a round trip: the agent compiles and links fine, then hands John a `xcodebuild` command to paste into Terminal.app because signing fails from a non-GUI session. Verified 2026-07-26, not assumed: `xcodebuild -project App/WAIQ-iOS/WAIQ/WAIQ.xcodeproj -scheme WAIQ -destination 'id=<device>' -allowProvisioningUpdates build` reaches `CodeSign` and fails with `errSecInternalComponent` on `WAIQ.app/WAIQ.debug.dylib`; compilation, linking, and asset copying all succeed. Cause is `securityd` refusing to release the "Apple Development: John DiFini (42NVR85YD8)" private key to a session without GUI login context. Two standard fixes, both needing John's login password once and therefore his explicit decision: (1) `security unlock-keychain -p <pw> ~/Library/Keychains/login.keychain-db` at the start of a build, or (2) `security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k <pw> ~/Library/Keychains/login.keychain-db` to add `codesign` to the key's ACL permanently. Evaluate the security trade-off of (2) — it grants any local process holding the keychain the ability to sign as John — and consider whether a dedicated build-only signing identity is the safer shape. Everything downstream of signing is already automated: `xcrun devicectl` handles install, launch, and pulling the diagnostic log and crash reports. Relates to `repos/waiq/.claude/rules/on-device-verify-handoff.md`, which currently codifies the manual handoff as the expected workflow.

- [ ] **Bootstrap a greenfield mobile app that generates a personalized story** (architetto, added 2026-07-25) — Initialize a new independent repo under `repos/` (per `architetto/AGENTS.md`: interview for constraints, pick stack/test framework/layout/persistence from the approved menu, write ADRs + the repo's `AGENTS.md`/`CLAUDE.md`, symlink `.claude/commands`, gitignore it). Product spec: a mobile app whose onboarding flow (1) asks "Tell me stories that you would like me to base the new story on." — freeform, e.g. The Kingkiller Chronicle, A Song of Ice and Fire, the original Star Wars trilogy; (2) asks eight multiple-choice questions about what the user liked and disliked about those stories; (3) asks whether they want an audiobook — assume audiobook output must be very short given TTS compute cost, so gate length hard when selected; (4) if no audiobook, asks for desired book length; (5) generates an original story from the answers. Hard constraint: the generated story must not infringe copyright — the named works are inspiration signals (tone, themes, structure, tropes) only, never source text, character names, proper nouns, or setting names; design the prompt pipeline and any guardrail/eval step around that from the start and record it as an ADR. Open questions for the interview: target platform (iOS/Android/cross-platform), which LLM + TTS providers, on-device vs. server inference, and whether generated stories persist per user.

- [ ] **Verify Codex's current slash-command syntax and migrate the bridge if it changed** (workspace, added 2026-07-25) — John reports `/prompts:<command>` no longer works in Codex and suspects invocation moved to `$<command>`. First confirm against current Codex CLI docs/release notes what the supported custom-prompt invocation actually is (Codex's custom-prompt surface was already deprecated in favor of skills when the bridge was built, so the answer may be "migrate to skills" rather than a new prefix). If it changed, update the bridge and every place that hardcodes the old syntax: `.bin/sync-codex-prompts.sh` (the `/prompts:%-28s` listing on line 70 and its `--check` mode), root `AGENTS.md` line 109 (Lint → Slash commands), and `README.md` line 61. Then re-run `.bin/sync-codex-prompts.sh` and confirm `--check` passes. Follow-on to the completed "Make slash commands work in both Codex and Claude Code" entry.

- [ ] **Add a Haiku-backed `/done` session-closeout command** (workspace, added 2026-07-20) — Unless Claude Desktop already ships an equivalent, create a `/done` slash command that uses the inexpensive Haiku model to acknowledge and close out a finished thread (user signals "I'm done, nothing to do" → brief "Thread closed" ack, no re-interpretation of the closeout text as a task). Today the user falls back to `/h` for this, which mis-fires by treating "no" / "I'm done with this thread" as a prompt to evaluate rather than a close signal. Add to the canonical `.claude/commands/` (workspace-level so it's available in every agent), keep the body harness-neutral, then run `.bin/sync-codex-prompts.sh` and confirm `--check` passes. First step is the conditional check: verify Desktop has no built-in equivalent before building.

- [ ] **Add weekly CLI-update scheduled job** (c4po, added 2026-07-18) — launchd job under `com.theborg.*` that runs `codex update` and `claude update` so both CLIs stay current on the headless Mac Studio (no desktop logins). Claude Code's native install (`~/.local/share/claude/`) already auto-updates on every run, so it's belt-and-braces; Codex's standalone install (`~/.codex/packages/`) needs the explicit `codex update`. Note: this job invokes no model — it's a plain shell job, not a `.prompt`-driven task, so decide whether the lint rule's `.prompt`↔slash-command pairing applies or document the exemption in the runner/README.

- [ ] **Add Medium.com as a knowledge source** (cerebruh, added 2026-07-19) — Evaluate wiring Medium.com in as a source for Cerebruh. Open questions to resolve: (1) **Access** — John has a paid Medium subscription; determine how to authenticate against member-only content (session cookie, RSS, official API surface, or logged-in browser session) without leaking credentials. (2) **Libraries** — decide whether to use open-source Medium scraping/parsing libraries; if so, run them through the workspace skill/dependency security vetting (`/vet-skill`, dependency scan) before adoption. (3) **Routing logic** — define how an agent decides among Cerebruh, the live web (`/last30days`, WebSearch), and its own existing knowledge base for a given question: precedence, when to fall through, and how ingested Medium content is cited. (4) **Staleness** — many articles already in Cerebruh were sourced from Medium and are ephemeral snapshots; they likely drifted from what's currently live on Medium (edits, updates, retractions). Decide whether/how to detect staleness (last-ingested date vs. article age, periodic re-check) and whether routing logic should prefer a fresh Medium fetch over a stale Cerebruh page for time-sensitive topics. Note Cerebruh is read-only to agents — new sources enter only via Cerebruh's injection-scanned ingest workflow, so the Medium pipeline must run inside that.

- [x] **Make slash commands work in both Codex and Claude Code** (workspace, added 2026-07-18, done 2026-07-19) — `.claude/commands/*.md` remains canonical; `.bin/sync-codex-prompts.sh` safely symlinks every live Borg command into `~/.codex/prompts/`, scope-prefixing collisions and refusing to overwrite unrelated prompts. Claude invokes `/name`; Codex's reserved namespace requires `/prompts:name`. The bridge has `--check` lint coverage, README/setup documentation, and harness-neutral model-alias bodies. Codex custom prompts are deprecated, so the monthly assumptions audit now watches for a supported replacement.

- [x] **Restore the consolidate-memory skill for the weekly memory job** (c4po, added 2026-07-13, closed 2026-07-14) — Obsolete: the user decommissioned the `c4po-consolidate-memory` job entirely (launchd plist, `.prompt`, `/consolidate-memory` command, state file, and install-script row removed) instead of restoring the vanished built-in skill.

- [ ] **Assess Google's Open Knowledge Format (OKF) impact on Cerebruh** (cerebruh, added 2026-07-10) — Research Google's recent OKF announcements, then analyze whether Cerebruh's LLM-Wiki logic or structure should change to adopt or interoperate with it: sub-wiki layout, page frontmatter/format, ingest workflow, and citation model in `cerebruh/CLAUDE.md` and `cerebruh/wikis/`. Email findings to John (johndifini@gmail.com). Relates to the prior "is the LLM Wiki pattern still the best lightweight RAG fit" audit (commit `4c825b2`).

- [x] **Create pre-reset backlog-burndown scheduled job** (c4po, added 2026-07-05, done 2026-07-05) — New scheduled job that fires ~4 hours before the account's weekly Claude usage reset (fixed day/time per account — check `/status` in Claude Code or claude.ai Settings > Usage to pin the concrete time, then schedule reset-minus-4h). The job: (1) prioritizes every backlog — the workspace `BACKLOG.md` plus any backlogs inside the independent repos under `repos/*`; (2) builds a consolidated, prioritized union of all backlog items; (3) starts implementing the highest-priority item in auto mode, then loops through remaining items until credits run out. Per workspace lint rules, implement as a launchd job under `com.theborg.*` driven by a `.prompt` file, with a paired interactive slash command in `c4po/.claude/commands/` that delegates to the same `.prompt`.

- [x] **Extend daily security audit to cover `repos/`** (c4po, added 2026-07-04, done 2026-07-05) — `c4po-security-audit.prompt` (`c4po/.claude/scheduled/c4po-security-audit.prompt`) only reviews Borg-agent `settings*.json`, hooks, permission rules, Telegram access, and MCP servers. Confirmed via the run logs (`c4po/.claude/scheduled/logs/c4po-security-audit.log`) that every entry checks "7 settings files" and never mentions `repos/*`. The independent git repos under `repos/` (e.g. `repos/waiq`) have their own `.claude/` config, potential hooks, and secrets that go unaudited despite living inside `~/theborg`. Add a step to sweep each `repos/*/.claude/settings*.json`, hooks, and secret patterns — or explicitly document why they're intentionally out of scope.

- [x] **Make lint audit recursive into subdirectories and `repos/`** (c4po, added 2026-07-04, done 2026-07-05) — The monthly lint audit (`c4po/.claude/scheduled/c4po-assumptions-audit-monthly.prompt` / `/lint-audit`) evaluates the workspace lint rules from `CLAUDE.md` → Lint, but consider extending it to recurse into subdirectories — including the independent git repos under `repos/` (git-ignored, so they never surface in workspace git status) — so their CLAUDE.md files are checked against the same rules (coverage, size ceiling, cross-references, paths, imports). Decide whether `repos/*` should inherit workspace lint rules or define their own, and document the choice.

- [x] **Route design/taste/UI work through jony-vibe** (workspace, added 2026-07-05, done 2026-07-08, `4d7bdba`) — Establish the convention that all agents consult jony-vibe for design, taste, and UI changes (logos, color/type, layout, brand, visual polish) instead of deciding themselves. Codify it in the root `CLAUDE.md` so it loads in every agent's context, and verify `jony-vibe/CLAUDE.md` reflects its role as the consulted party.
